name: Deploy Reutilizável

on:
  workflow_call:
    inputs:
      environment:
        description: 'Ambiente para deploy (staging ou production)'
        required: true
        type: string
      version:
        description: 'Versão da aplicação'
        required: false
        type: string
        default: 'latest'
    secrets:
      deploy_token:
        description: 'Token para autenticação no deploy'
        required: false

jobs:
  deploy:
    name: Deploy para ${{ inputs.environment }}
    runs-on: ubuntu-latest
    environment: 
      name: ${{ inputs.environment }}
      url: ${{ steps.deploy.outputs.url }}
    
    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Instalar dependências
        run: npm ci

      - name: Build da aplicação
        run: |
          echo "🏗️  Construindo aplicação para ${{ inputs.environment }}..."
          echo "Versão: ${{ inputs.version }}"
          # Aqui você rodaria: npm run build
          npm run build || echo "Build executado (sem script definido)"

      - name: Executar deploy
        id: deploy
        run: |
          echo "🚀 Iniciando deploy para ${{ inputs.environment }}..."
          echo "📦 Versão: ${{ inputs.version }}"
          echo ""
          
          # Simulação de deploy baseado no ambiente
          if [ "${{ inputs.environment }}" == "staging" ]; then
            echo "🔧 Configurando ambiente de STAGING..."
            echo "URL: https://staging.exemplo.com"
            echo "url=https://staging.exemplo.com" >> $GITHUB_OUTPUT
          elif [ "${{ inputs.environment }}" == "production" ]; then
            echo "🔧 Configurando ambiente de PRODUÇÃO..."
            echo "URL: https://exemplo.com"
            echo "url=https://exemplo.com" >> $GITHUB_OUTPUT
          fi
          
          echo ""
          echo "✅ Deploy concluído com sucesso!"
          echo "🌍 Aplicação disponível em: ${{ steps.deploy.outputs.url }}"

      - name: Verificar saúde da aplicação
        run: |
          echo "🏥 Verificando saúde da aplicação..."
          echo "✅ Health check passou!"
          
      - name: Notificar sucesso
        if: success()
        run: |
          echo "✅ Deploy para ${{ inputs.environment }} concluído com sucesso!"
          echo "🔗 URL: ${{ steps.deploy.outputs.url }}"

      - name: Notificar falha
        if: failure()
        run: |
          echo "❌ Deploy para ${{ inputs.environment }} falhou!"
          echo "📋 Verifique os logs acima para mais detalhes."
          exit 1
